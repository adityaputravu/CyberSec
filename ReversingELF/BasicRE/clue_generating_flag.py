#flag = HIDDEN
flag = bytearray(map(ord,flag))

def bitflip(num):
    num = bin(num)[2:]

    return new

new  = []
for c in flag:
    new.append(c^0xff)

new_new = list(map(hex, new))

print(flag, new, len(new))
